---

#
# Example Call: ansible-playbook -i inventories/serverPi playbooks/pb_init_pistack.yml --limit server1Pi --vault-id @prompt
#

all:
 
  children:
    controllers:
      vars:    
        ansible_connection: local
        ansible_python_interpreter: /usr/bin/python3
      hosts:
        ansiblePi:
    servers:
       hosts:
        server1Pi:
        server2Pi:
        server3Pi:      
